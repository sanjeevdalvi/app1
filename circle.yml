machine:
  node:
    version: 6.0.0

deployment:

  stage:
    branch: master
          name: Deploy to S3 if tests pass and branch is Master
          command: aws s3 sync app1 s3://meply --delete
    